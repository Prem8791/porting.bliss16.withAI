# Extension service boundary

Reserved for authenticated extension discovery and lifecycle. Dynamic code loading, arbitrary plugin execution, and trust-boundary bypasses are outside the approved model.
