# Test hierarchy

P0-01 reserves separated host, device, contract, unit, integration, security, fuzz, fixture, and immutable reference boundaries. No test binary, harness, source, or build target is declared yet.

`reference/` is the P0-00 lock and must verify independently. Future fixtures must be deterministic and free of production credentials or personal data.
