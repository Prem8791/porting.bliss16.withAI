# Documentation

Architecture mappings belong in `architecture/`; milestone plans, reports, and validation evidence belong in `engineering/`. Frozen authoritative inputs remain in `tests/reference` and must not be rewritten here.
