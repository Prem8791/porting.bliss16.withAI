# Service boundaries

Each dedicated service owns a separate directory and future process/trust boundary. P0-01 creates documentation placeholders only; there are no service declarations, entry points, IPC endpoints, or installed modules.
