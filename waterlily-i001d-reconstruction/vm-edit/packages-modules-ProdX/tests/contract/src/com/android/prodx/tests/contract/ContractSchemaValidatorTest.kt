package com.android.prodx.tests.contract

import org.junit.Test
import org.junit.Assert.assertNotNull

class ContractSchemaValidatorTest {
    @Test fun testValidatorExists() { assertNotNull(Unit) }
}
